{
  "version": 1,
  "hooks": {
{{- if eq (index . "ccTtsEnabled" | default "false") "true" }}
    "stop": [
      {
        "command": "{{ .chezmoi.homeDir }}/.cursor/hooks/cursor-tts.sh",
        "timeout": 15
      }
    ]
{{- end }}
  }
}
