# Paths chezmoi removes from the target on apply (POSIX/$HOME side).
# wzr was absorbed into `doc` (docs/kb/wezterm/*); drop its old deployed files.
.local/bin/wzr
.wezterm-ref
# command-reference was replaced by the `doc` knowledge base (docs/kb/). Drop the
# old deployed cheat-sheet + generated twins. NOTE: command-reference.local.md is
# the user's untracked personal file — intentionally NOT removed.
command-reference.md
command-reference.txt
command-reference.html
# Installer entry points + sync script were previously deployed to $HOME because
# they weren't in .chezmoiignore. They belong only in the clone — drop the copies.
install-linux.sh
install-mac.sh
install-wsl.sh
install.ps1
scripts/sync-windows.ps1
